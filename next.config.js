/** @type {import('next').NextConfig} */
// const nextConfig = {}
const nextConfig = {
    // reactStrictMode: true,
    // swcMinify: true, // Activa el SWC minifier
    // compiler: {
    //   removeConsole: {
    //     exclude: ['error', 'warn'], // Opcional: conserva ciertos métodos de consola
    //   },
    // },
  };

module.exports = nextConfig
